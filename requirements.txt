# Core scientific stack
numpy
scipy
pandas
matplotlib
seaborn
scikit-learn
statsmodels

# Imaging / video / pose estimation
opencv-python-headless
pillow
tifffile
suite2p

# Modeling / optimization
torch
optuna
torch-geometric
keras
optkeras

# GUI (used headlessly for import-testing only; requires system Tk via apt)
customtkinter

# Misc utilities used across the package
tqdm
tqdm_joblib
venn
watchdog
psutil
networkx

# Testing
pytest

# NOTE: The following is used by one module but is intentionally left out of
# the base install because it is not published on PyPI under a matching name
# (the actual DeepCAD-RT project lives on GitHub, not PyPI):
#   - deepcad (denoise/RunDeepCAD.py)
# 'projectmanager' (utils/logger.py) is also a private, unpublished package;
# that import is now optional/lazy so the rest of the module still works
# without it (see Sweet2Plus/utils/logger.py).
